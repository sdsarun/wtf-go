package main

import "testing"

// TestCLI should be table-driven over every row of docs/01-unit-converter-cli.md's Case specification
// (both the SUCCESS and FAILURE tables) — see "Test requirements".
func TestCLI(t *testing.T) {
	t.Skip("TODO(you): implement — see docs/01-unit-converter-cli.md \"Test requirements\"")
}
